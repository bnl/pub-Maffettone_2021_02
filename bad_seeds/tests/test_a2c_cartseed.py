from bad_seeds.training.train_a2c_cartseed import set_up
from bad_seeds.training.train_a2c_cartseed import main as training_main


def test_defaults(default_setup):
    pass


def test_outputs(tmp_path):
    """Run training_main with short arguments and ensure the summary and models are output to desired paths"""
    pass
